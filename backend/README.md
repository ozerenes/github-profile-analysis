# Professional Presence Analyzer — Backend (API)

Node.js API for CV and profile analysis.

## Requirements

- Node.js 18+

## Setup

```bash
npm install
cp .env.example .env
# Configure .env (PORT, etc.)
```

## Run

```bash
npm start
# or watch: npm run dev
```

Default port: **3001** (set `PORT` to override).
